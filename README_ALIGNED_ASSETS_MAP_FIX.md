# The Report — Aligned Assets + Map Fix 019

Includes one single aligned ABCD map, strict building/water collision, Malcolm/Ramos corrected walk sheets, aim-pose sheets, weapon overlays, and Malcolm Monte Carlo car.

Install:
```bash
unzip -o the_report_aligned_assets_map_fix.zip
git add index.html assets config docs README_ALIGNED_ASSETS_MAP_FIX.md
git commit -m "Fix aligned map and import updated sprites"
git push
```

Test:
https://adonisdross.github.io/The-Report/?v=aligned-assets-map-019
